/**
 * Task surface for the compare change — `# Spec: compareCmd`.
 *
 * Re-export of the side-by-side comparison so the capability resolves in the
 * drift check regardless of how shield scans exports.
 */
export { compareCmd } from "../cli/compareCmd.js";
