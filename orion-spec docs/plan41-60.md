# Orion Implementation Plan – Steps 41‑60

**2️⃣ YAGNI‑ladder (orion‑scale)**
41. В `reuse.ts` реализовать поиск одинаковых функций в проекте (fast‑glob) и замену на импорт.
42. В `stdlib.ts` парсить AST (via @babel/parser) и добавлять префикс `node:` к системным модулям.
43. В `native.ts` заменять синхронные вызовы (`fs.readFileSync`) на асинхронные (`fs.promises.readFile`).
44. В `dep.ts` проверять `package.json` – если зависимость отсутствует, добавить её в `devDependencies` и выполнить `pnpm install`.
45. В `oneLiner.ts` упрощать длинные строки >80 симв в тернарные/опциональные цепочки.
46. В `minimum.ts` удалять `console.*`, `debugger`, комментарии и пустые строки.
47. В `src/core/scale.ts` реализовать `async applyScale(code)` – загружать стадии из `orionScale.json`, последовательно вызывать их `handler`.
48. Добавить кэширование в `applyScale` – перед каждой стадией проверять `OrionTrack.load('scale:'+stage+':'+hash)`.
49. В `src/cli/commands.ts` добавить под‑команду `scale <file> [--dry]` (чтение файла, применение, вывод/сохранение).
50. При `--dry` выводить список стадий и diff‑preview (через `diff`‑пакет).
51. Добавить unit‑тест `scale.test.ts` – проверка каждой стадии на простом примере.
52. Добавить e2e‑тест `cli/scale.e2e.test.ts` (CLI, кеш‑проверка).
53. Обновить `README.md` секцию **Scale (YAGNI ladder)** с таблицей стадий и примерами.
54. В `src/type.ts` добавить типы `ScaleConfig`, `ScaleStage`.
55. В `src/config/orionScale.json` задать массив `stages` с именами стадий (`yagni`, `reuse`, `stdlib`, `native`, `dep`, `oneLiner`, `minimum`).
56. Добавить скрипт `pnpm run format` в package.json (Prettier) и убедиться, что `src/scaleStages/*` отформатированы.
57. Добавить **CI‑шаг** `npm run lint && npm run format --check` перед сборкой.
58. Добавить `src/utils/sha256.ts` (вспомогательная функция для хешей кода).
59. В `src/core/scale.ts` реализовать функцию `hashCode(code)` → `sha256(code)`.
60. В `applyScale` сохранять результат каждой стадии в кеш с ключом `scale:<stage>:<hash>`.
