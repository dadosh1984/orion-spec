# Orion Implementation Plan – Steps 21‑40

**1️⃣ Базовая архитектура проекта**
21. В `src/cli/commands.ts` добавить регистрацию команд (`think`, `draft`, `forge`, `shield`, `track`, `scale`, `tdd`, `out`).
22. В `src/cli/commands.ts` реализовать вывод справки при неизвестной команде.
23. В `src/utils/hash.ts` реализовать `export const sha256 = (data:string)=>createHash('sha256').update(data).digest('hex');`.
24. В `src/utils/file.ts` добавить функции `readFile(path)`, `writeFile(path, data)`, `ensureDir(dir)`.
25. В `src/core/track.ts` объявить `class OrionTrack` с полем `cacheDir = path.join(os.homedir(),' .orion','cache')` и конструктором, создающим директорию.
26. В `OrionTrack` реализовать метод `entryPath(key)` → `${cacheDir}/${key}.json`.
27. Реализовать `store(key,value)` (записывает JSON с timestamp).
28. Реализовать `load(key)` (чтение, возврат value или null).
29. Реализовать `exists(key)` (fs.existsSync).
30. Реализовать `prune()` (удалять старые файлы пока суммарный размер ≤ maxSize из `orionTrack.json`).
31. Реализовать `batch(keys[])` (параллельно load и возвращать объект `{key:value}`).
32. Добавить `static init()` – читает `src/config/orionTrack.json` и возвращает экземпляр.
33. В `src/cli/commands.ts` добавить под‑команды `track status`, `track prune`, `track get <key>`, `track set <key> <value>`.
34. Добавить unit‑тест `track.test.ts` (store, load, prune при переполнении).
35. Добавить e2e‑тест `cli/track.e2e.test.ts` (запуск CLI‑команд).
36. Обновить `README.md` секцией **Cache (track)** с примерами.
37. В `src/type.ts` добавить тип `TrackConfig`.
38. В `src/config/orionTrack.json` задать `{ "maxSize": 104857600, "ttlDays": 30 }`.
39. Добавить `src/scaleStages/` с файлами `yagni.ts`, `reuse.ts`, `stdlib.ts`, `native.ts`, `dep.ts`, `oneLiner.ts`, `minimum.ts` (каждый экспортирует `handler(code:string)`).
40. В `yagni.ts` реализовать `return code;` (ничего не меняет).
