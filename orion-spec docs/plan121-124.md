# Orion Implementation Plan – Steps 121‑124

**6️⃣ CI / CD, Publishing, Documentation, Future Extensions**
121. Создать `.github/workflows/ci.yml` с шагами: install, lint, type‑check, test (coverage), track‑prune, build, upload‑artifact.
122. Добавить в workflow `actions/upload-artifact@v4` для папки `dist/` и `orion-report.json`.
123. Добавить `README.md` badge CI‑status, инструкцию по запуску `pnpm run ci` локально, и ссылку на CI‑лог.
124. Настроить публикацию npm‑пакета: в `package.json` добавить `"bin":{"orion":"./dist/cli/index.js"}`, `"files":["dist","src","README.md","LICENSE"]`, `"publishConfig":{"access":"public"}`.  Добавить скрипт `publish` (`npm publish`).
