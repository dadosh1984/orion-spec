/**
 * GREEN — D2: README обновлён.
 *
 * Секция Installation уже содержала `npm i -g orion-spec`, local dep,
 * from-source и Updating; добавлен абзац про killer-фичу Honest Receipt
 * (секция в result.md из `orion out` + `receipt.json`) и его честность
 * (coverage "not measured"). Бейдж `img.shields.io/npm/v/orion-spec` уже в
 * шапке.
 */
