/**
 * GREEN — control: полный гейт CI эквивалент.
 *
 * vitest 73+ файлов, eslint/tsc/build плюс новый tests/distribute-package.
 * Все зелёные; core:coverage проходит. Обновление версии/публикация в npm
 * выполняется только при доступных NPM_TOKEN/GH_TOKEN (шаг 8).
 */
