/**
 * GREEN — control: формат/стиль в порядке.
 *
 * `pnpm lint` (eslint --max-warnings=0) и `pnpm format:check` (prettier)
 * зелёные после правок package.json/lock/README. Ни одна правка не задевает
 * runtime-логику — только метаданные пакета и документ.
 */
