/**
 * GREEN (deferred по условию) — Фаза 3: эмбеддинги.
 *
 * Условие запуска из tasks.md: «ТОЛЬКО если миss-лог покажет системный провал
 * BM25 на перефразированных шагах». Проверено по `readMissLog()` на живых
 * данных сессии 2026-08: сигнала системного провала BM25 в миss-логе НЕТ —
 * повторяющиеся сигнатуры успешно уходят в промоушен (promotionCandidates),
 * перепад BM25-скора на перефразировках ниже консервативного порога 0.45
 * не срабатывает как провал.
 *
 * Поэтому эмбеддинги НЕ разворачиваются (YAGNI rung 3: ноль внешних
 * зависимостей — эмбеддинг-модель это тяжёлое добавление). Инфраструктура
 * сигнала уже есть: миss-лог пишется с первого дня, promotionCandidates
 * находит повторения. Когда/если перефразировки начнут системно не совпадать —
 * миss-лог это покажет точным числом, сканер запустится по нему.
 *
 * Решение согласовано с развилкой v0.51 «не строить всё сразу».
 */
