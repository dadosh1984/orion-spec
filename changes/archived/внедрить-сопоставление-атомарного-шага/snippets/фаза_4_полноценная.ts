/**
 * GREEN (deferred по условию) — Фаза 4: полноценная инвалидация по
 * environmentFingerprint.
 *
 * Условие запуска из tasks.md: «при живом кейсе миграции схемы» (напр.
 * 1C_TI → 1C_TI_NEW). Отпечаток уже ПИШЕТСЯ с первого дня: RunManifest
 * carries environmentFingerprint, matchSkill понижает до ambiguous skill с
 * отличающимся отпечатком даже при высоком BM25-score (реализовано в ядре,
 * задаче «env-fingerprint теперь живёт»), fill domain/envfp при создании.
 *
 * То есть half-инвалидация живёт: отличающийся отпечаток не даёт уверенного
 * USE_SKILL. Полная инвалидация (авто-игнор/пересбор idf по новому корпусу
 * схема-миграции) НЕ разворачивается, т.к. живого кейса миграции схемы в
 * рабочих данных до сих пор не было — строить её значит писать код под
 * гипотезу без воспроизводимого триггера (YAGNI rung 6).
 *
 * При появлении реального кейса (1C_TI_NEW в миss-логе с подъёмом
 * false-positive соответствий) — развёртывание пойдёт по этому snippet-условию.
 */
