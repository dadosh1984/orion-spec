# Spec: node

Платформенный контракт: node >= 22, CLI + MCP stdio, ноль зависимостей.
Реализация capability `node` — см. `src/tasks/node.ts` (token economy, v0.11).
Функциональные требования v0.12 живут в `specs/self/spec.md`.
